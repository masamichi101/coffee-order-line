{
  "scripts": {
    "build": "echo 'Skipping Tailwind build in production' || exit 0",
    "watch": "tailwindcss -i ./static/css/tailwind.css -o ./static/css/style.min.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
